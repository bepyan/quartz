---
title: 스쿼드
date: 2025-02-12
tags:
  - seed
---

Spotify에서 만든 조직 운영 방식이다.

목적조직? 하나의 작은 실행 단위의 조직

한국어로는 분대?이다.
https://namu.wiki/w/%EB%B6%84%EB%8C%80?from=%EC%8A%A4%EC%BF%BC%EB%93%9C


```
  import { tick } from 'svelte';

  let inputRef = $state<HTMLInputElement | null>(null);
  let isShow = $state(false);

  function handleClickSearch() {
    isShow = true;
    tick().then(() => {
      inputRef?.focus();
    });
  }

  <form action="/search" method="get">
    <input
      bind:this={inputRef}
      name="q"
      autocomplete="off"
      placeholder="검색어를 입력해 주세요"
      type="text"
      value=""
    />
  </form>
```


```

  .wrap_btn_search {
    input {
      display: none;
    }
  }

  .wrap_btn_search.show {
    position: relative;
    top: -7px;
    right: -16px;
    width: 204px;
    height: 34px;
    border: 1px solid #ddd;
    border-radius: 20px;
    background: #fff;

    input {
      border: 0;
      display: inline-block;
      font-size: 12px;
      height: 20px;
      line-height: 18px;
      margin-left: 40px;
      margin-top: 6px;
      outline: none;
      width: 138px;
    }
  }
```


```
import { atom } from 'nanostores';

type AuthorityType = 'ROLE_MANAGE_PROFILE' | 'ROLE_PUBLISH' | 'ROLE_WRITE_COMMENT';

type User = {
  userId: string;
  version: null;
  no: number;
  userName: string;
  userNameUpdateTime: number;
  userImage: string;
  email: null;
  password: string;
  emailValidation: boolean;
  agreeCondition: boolean;
  agreeDirectMail: boolean;
  agreeAdditionalInfo: boolean;
  additionalInfoUpdateTime: null;
  showAgreeAdditionalInfoPopup: boolean;
  agreeContactEmail: boolean;
  agreeContactEmailUpdateTime: null;
  profileId: string;
  profileImage: null;
  description: string;
  home: string;
  notificationHome: string;
  createTime: number;
  updateTime: number;
  useApp: boolean;
  publishCount: number;
  profileUpdateTime: number;
  status: string;
  restrictTime: null;
  activityTime: null;
  changeEmail: boolean;
  changeEmailAddress: null;
  followerCount: number;
  articleCount: number;
  publishTime: null;
  connection: null;
  highlight: null;
  author: boolean;
  company: string;
  descriptionDetail: string;
  profileNotiExpireTime: number;
  kakaoAccountConfirmed: boolean;
  kakaoAccountConfirmedTime: number;
  mode: null;
  useBrunchbook: boolean;
  enabledDonation: boolean;
  donationLegalComplianceAgreed: boolean;
  keywordProfileCategoryList: null;
  membership: null;
  authorities: { authority: AuthorityType }[];
  accountNonExpired: boolean;
  accountNonLocked: boolean;
  credentialsNonExpired: boolean;
  enabled: boolean;
  username: string;
  userLevel: string;
  restrict: boolean;
  sleep: boolean;
  authorAndEmailValidation: boolean;
  authorAndNotEmailValidation: boolean;
  allowedDonation: boolean;
  notEmailValidation: boolean;
  jobKeywordList: string[];
  subjectKeywordList: string[];
};

export const userStore = atom<User | null>(null);

```